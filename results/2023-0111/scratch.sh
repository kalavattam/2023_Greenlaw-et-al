#!/bin/bash

#  scratch.sh
#  KA

for min_glue in 1 2 4; do
    for glue_factor in 0.005 0.01 0.05 0.1; do
        echo "Permutation #1"
        echo "-   --ming_glue:  ${mg}"
        echo "- --glue_factor:  ${gf}"
        echo ""
    done
done
